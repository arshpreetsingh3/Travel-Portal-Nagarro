import { AdminLoginDetails } from './admin-login-details';

describe('AdminLoginDetails', () => {
  it('should create an instance', () => {
    expect(new AdminLoginDetails()).toBeTruthy();
  });
});
